name: Docker Image CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: write

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER_NAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            samsam067/flask:latest
            samsam067/flask:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  update_deployment:
    runs-on: ubuntu-latest
    needs: build_and_push
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update image tag with sed
        run: |
          IMAGE_NAME="samsam067/flask"
          NEW_TAG="${{ github.sha }}"
          FILE="deployment/deploy.yml"

          # Utilisation de sed pour modifier la ligne de l'image
          sed -i "s|image: $IMAGE_NAME:.*|image: $IMAGE_NAME:$NEW_TAG|g" "$FILE"

          echo "Vérification après modification :"
          grep "image: $IMAGE_NAME" "$FILE"

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          git add deployment/deploy.yml

          # Commit seulement s'il y a une différence réelle
          if git diff --cached --quiet; then
            echo "Rien à mettre à jour."
          else
            git commit -m "🚀 Deploy: update image tag to ${{ github.sha }}"
            git push origin main
          fi
